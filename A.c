#include<stdio.h>
#include<math.h>

int main(){
     int t;
    while(t--)
    {
        int n,d;
        int res;

        scanf("%d%d",&n,&d);
        res=d/n;
        printf("%d",res);
       // if(ceil(res)>2)

    }
    return 0;
}
